{
    "name": "webos-api",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "cargo run",
        "build": "cargo build --release",
        "start": "target/release/webos-api",
        "lint": "cargo clippy"
    }
}