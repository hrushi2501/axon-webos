# Axon OS Frontend

This is the Next.js frontend for Axon OS.

## Development

The recommended way to run this application is from the project root using:

```bash
npm run dev
```

This ensures the backend API is also started.

If you want to run **only** the frontend (without the backend features like Task Manager stats):

```bash
cd apps/web
npm run dev
```
